{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix web\"",
    "build": "npm run build --prefix web",
    "deploy": "gh-pages -d dist/apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "gh-pages": "^6.1.0"
  },
  "workspaces": ["web"]
}